---
platform: junos

creds_core_ssh:
  host: "{{ ansible_host }}"
  username: "{{ device_ssh_user }}"
  password: "{{ device_ssh_password }}"
  transport: cli
  timeout: 60

creds_core_netconf:
  host: "{{ ansible_host }}"
  username: "{{ device_ssh_user }}"
  password: "{{ device_ssh_password }}"
  transport: netconf
  timeout: 60

creds_napalm:
  hostname: "{{ ansible_host }}"
  username: "{{ device_ssh_user }}"
  password: "{{ device_ssh_password }}"
  dev_os: "junos"
  optional_args: {}
